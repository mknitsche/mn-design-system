# Node
node_modules/
npm-debug.log*
.npm/

# Build artefacts: dist/ wird COMMITTED (Konsumenten brauchen die CSS/JSON/LaTeX-Outputs)
# ABER: Python-Wheels gehoeren in GitHub-Releases, nicht ins Repo —
# eingecheckte alte Wheels haben in v0.5.4 (S251 KT-1) Multi-Version-Konflikte
# im release.yml-Smoke-Test ausgeloest. dist/*.whl ab jetzt gitignored.
dist/*.whl
dist/*.tar.gz
package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Python build artefacts (Welle A v0.1.0)
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/python/
.pytest_cache/

# Fonts: ab v0.1.0 IM Repo (mn_design_system/fonts/, alle OFL 1.1 / MIT,
# Public-tauglich, self-contained fuer pip install -e + Multi-KI-Edit).
# Top-Level fonts/install.sh-Workflow ist deprecated.
